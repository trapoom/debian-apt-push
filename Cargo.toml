license = "MIT"
repository = "https://github.com/trapoom/debian-apt-push"
homepage = "https://trapoom.github.io/debian-apt-push/"

[package]
name = "moniter"
version = "0.1.0"
edition = "2021"

[dependencies]
libc = "0.2"

[profile.release]
opt-level = 2

